import { defaultTheme } from '@medly-components/theme';
import { Button } from '@medly-components/core';
import { Preview, Meta, Props, Story } from '@storybook/addon-docs/blocks';
import * as stories from './Colors.stories.tsx';

<Meta title="Colors" parameters={{ previewTabs: { 'storybook/docs/panel': { index: -1 } } }} />

# Colors

These colors can be used to create a color theme that reflects your brand or style.

### How to use

```tsx
import { defaultTheme } from '@medly-components/theme';

const color = defaultTheme.colors.medlyGreen[500];
```

### Colors with single shade

<Preview withToolbar>
    <Story name="SingleShadeColors">{stories.SingleShadeColors()}</Story>
</Preview>

### Colors with multiple shades

<Preview withToolbar>
    <Story name="MultiShadeColors">{stories.MultiShadeColors()}</Story>
</Preview>

### GreyScale

<Preview withToolbar>
    <Story name="GreyScale">{stories.GreyScale()}</Story>
</Preview>
