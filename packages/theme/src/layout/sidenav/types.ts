import { FontSizes, IconSizes } from '../../core/types';

export interface SideNavTheme {
    closeSize: string;
    openSize: string;
    iconSize: IconSizes;
    textSize: FontSizes;
    bgColor: string;
    textColor: string;
    activeColor: string;
}
