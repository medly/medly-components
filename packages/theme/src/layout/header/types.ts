export interface HeaderTheme {
    backgroundColor: string;
    maxContentWidth: string;
    height: string;
    fontColor: string;
    navItem: {
        fontColor: string;
        activeColor: string;
        hoverColor: string;
        activeIndicatorColor: string;
        height: string;
    };
}
