export interface CheckboxSizes {
    XS: string;
    S: string;
    M: string;
    L: string;
    XL: string;
}

export interface CheckboxTheme {
    sizes: CheckboxSizes;
    defaultSize: string;
    fillColor: string;
    bgColor: string;
    borderColor: string;
}
