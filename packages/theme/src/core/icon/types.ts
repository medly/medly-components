export type IconSizesType = 'XS' | 'S' | 'M' | 'L' | 'XL';

export interface IconTheme {
    sizes: {
        XS: string;
        S: string;
        M: string;
        L: string;
        XL: string;
    };
    colorPrimary: string;
}
