import colors from '../colors';

const SearchBox = {
    bgColor: colors.white,
    textColor: colors.grey[500],
    borderColor: colors.grey[500],
    boxShadow: colors.grey[600]
};

export default SearchBox;
