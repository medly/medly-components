import colors from '../colors';
import { ToastTheme } from './types';

const toast: ToastTheme = {
    successColor: colors.green[200],
    infoColor: colors.communityBlue[200],
    warningColor: colors.sunshineYellow[200],
    errorColor: colors.red[200],
    textColor: colors.professionalNavy
};

export default toast;
