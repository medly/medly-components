export interface TabsTheme {
    tablistBgColor: string;
    textColor: {
        active?: string;
        nonActive?: string;
        hovered?: string;
    };
    backgroundColor: {
        active?: string;
        nonActive?: string;
        hovered?: string;
    };
}
