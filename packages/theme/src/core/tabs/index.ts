import colors from '../colors';
import { TabsTheme } from './types';

const tabs: TabsTheme = {
    shadowColor: 'rgba(0,0,0,0.2)',
    tablistBgColor: colors.white,
    activeTabTextColor: colors.orange[400],
    disabledTabTextColor: colors.grey[400]
};

export default tabs;
