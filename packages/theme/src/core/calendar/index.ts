import colors from '../colors';
import { CalendarTheme } from './types';

const card: CalendarTheme = {
    bgColor: colors.white,
    borderRadius: `0.4rem`,
    shadowColor: colors.grey[400]
};

export default card;
