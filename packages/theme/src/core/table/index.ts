import colors from '../colors';
import { TableTheme } from './types';

const table: TableTheme = {
    evenLiBgColor: colors.grey[50],
    oddLiBgColor: colors.white,
    borderColor: colors.grey[200]
};

export default table;
