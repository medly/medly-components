import { FontSizes, FontWeights } from '../font/types';

export interface LabelTheme {
    defaults: {
        textWeight: FontWeights;
        textSize: FontSizes;
        textColor: string;
    };
}
