export interface InputTheme {
    color: string;
    bgColor: string;
    borderColor: string;
    desciptionColor: string;
    disabledBgcolor: string;

    focus: {
        borderColor: string;
    };

    placeHolderColor: string;
}
