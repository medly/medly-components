export interface DialogBoxTheme {
    overlayColor: string;
    shadowColor: string;
    headerColor: string;
    backgroundColor: string;
    borderRadius: string;
    scrollbarThumbColor: string;
}
