import { HTMLProps } from '@medly-components/utils';
import SvgLoader from './SvgLoader';
import { defaultTheme } from '@medly-components/theme';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './Loaders.stories';

<Meta title="Loaders" component={SvgLoader} />

# Loaders

The `Loader` is a component that provides state during data loading. 

You can directly import loaders from `@medly-components/loaders`

<Preview withToolbar>
    <Story name="Loaders">{stories.Basic()}</Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={stories.Basic} />

## LoaderTheme

This component offers the following themes mentioned in the table below:

<Props of={stories.ThemeInterface} />
