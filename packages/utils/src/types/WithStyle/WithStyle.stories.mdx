import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Utils/Types/WithStyle" />

# WithStyle

Constructs a component type with the `Style` prop as the component itself.

#### Example

This utility type can be used to override specific styles of the components.

```ts
import { WithStyle } from '@medly-components/utils';
import { memo } from 'react';
import { Wrapper } from './Content.styled';
import type { FC } from 'react';

const Component: FC = memo(props => {
    return <Wrapper {...props} />;
});
Component.displayName = 'Content';
export const Content: FC & WithStyle = Object.assign(Component, { Style: Wrapper });
```
