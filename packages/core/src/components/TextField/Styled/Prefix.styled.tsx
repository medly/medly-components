import { InjectClassName, styled } from '@medly-components/utils';

export const Prefix = styled(InjectClassName)`
    margin: 0;
    padding: 0;
    position: absolute;
    top: 50%;
    left: 1.6rem;
    transform: translateY(-50%);
`;
