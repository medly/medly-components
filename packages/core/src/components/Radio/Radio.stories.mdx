import { Radio } from './Radio';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import { sizes, labelPositions, ThemeInterface } from './Radio.stories';
import { boolean, select } from '@storybook/addon-knobs';

<Meta
    title="Core"
    component={Radio}
    parameters={{
        jest: ['Radio.test.tsx'],
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/vA4EQdxl0d0ETtYpw2uSjW/Medly-Pattern-Library-B2B-Internal?node-id=239%3A0'
        }
    }}
/>

# Radio

The `Radio` component allows you to select a single option from a set. Use the radio component when all available options must be visible to the user.

<Preview withToolbar>
    <Story name="Radio">
        <Radio
            value="female"
            name="gender"
            label="Female"
            disabled={boolean('Disabled', false)}
            fullWidth={boolean('Full Width', false)}
            size={select('Size', sizes, 'S')}
            hasError={boolean('Has Error', false)}
            labelPosition={select('Label Position', labelPositions, 'right')}
        />
    </Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={Radio} />

## RadioTheme

This component offers the following themes mentioned in the table below:

<Props of={ThemeInterface} />
