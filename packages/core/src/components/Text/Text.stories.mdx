import { Text } from './Text';
import { defaultTheme } from '@medly-components/theme';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './Text.stories';

<Meta title="Core/Text" component={Text} />

# Text

It renders text in `span` html tag when text weight is `light` | `normal` and uses `strong` html tag for rendering text with`strong` text weight.

<Preview withToolbar>
    <Story name="Basic">{stories.Basic}</Story>
</Preview>

### Props

<Props of={Text} />

### TextTheme

<Props of={stories.ThemeInterface} />

### TextTheme Defaults

<code>{JSON.stringify(defaultTheme.text, null, 2)}</code>
