import { DialogBox } from './DialogBox';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './DialogBox.stories';

<Meta title="Core" component={DialogBox} parameters={{ jest: ['DialogBox.test.tsx'] }} />

# DialogBox

`DialogBox` are windows that appear in front of app content to give critical information, request a decision, or involve multiple tasks.  It consists of a Header, Content, and Action.

You can play with the component in the canvas tab.

```typescript
<DialogBox open={openState} onCloseDialogBox={changeDialogBoxState(false)}>
    <DialogBox.Header>Add User</DialogBox.Header>
    <DialogBox.Content>
        <Input id="name-input" type="text" fullWidth label="Name" placeholder="Enter your Name" />
    </DialogBox.Content>
    <DialogBox.Actions>
        <Button variant="outlined">Cancel</Button>
    </DialogBox.Actions>
</DialogBox>
```

Shadow appears on the Header and Actions section if you scroll through the content.

To close the dialog box, follow the steps given below:

1. Click the cross icon.
2. Press `Esc` key.
3. If the `shouldCloseOnOutsideClick` prop is selected, then dialog box will close on clicking outside the dialog box.

<Preview withToolbar>
    <Story name="DialogBox">{stories.Basic()}</Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={DialogBox} />

## DialogBox Action Props

The DialogBox action prop is given in the table below:

<Props of={stories.DialogBoxActionProps} />

## DialogBoxTheme

This component offers the following themes mentioned in the table below:

<Props of={stories.ThemeInterface} />
