import { List } from './List';
import { defaultTheme } from '@medly-components/theme';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './List.stories';

<Meta title="Core" component={List} />

# List

The `List` component allows you to display a list of elemsts like a paragraph, links, headings, etc.


<Preview withToolbar>
    <Story name="List">{stories.Basic()}</Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={List} />


