import { BreadCrumbs } from './BreadCrumbs.tsx';
import { defaultTheme } from '@medly-components/theme';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './BreadCrumbs.stories';

<Meta title="Core/BreadCrumbs" component={BreadCrumbs} />

# BreadCrumbs

### Breadcrumbs allow users to make selections from a range of values.

<Preview withToolbar>
    <Story name="Basic">{stories.Basic}</Story>
</Preview>

### Props

<Props of={BreadCrumbs} />

### BreadCrumbsTheme

<Props of={stories.ThemeInterface} />

### BreadCrumbsTheme Defaults

<code>{JSON.stringify(defaultTheme.breadCrumbs, null, 2)}</code>
