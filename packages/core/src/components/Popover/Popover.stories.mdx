import { Popover, PopoverWrapper } from './Popover';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './Popover.stories';

<Meta title="Core/Popover" component={Popover} />

# Popover

You have to use both `Popover` & `PopoverWrapper` component.

```typescript
import { Popover, PopoverWrapper } from '@medly-components/core';
```

<Preview withToolbar>
    <Story name="Basic">{stories.Basic()}</Story>
</Preview>

### PopoverWrapper Props

<Props of={PopoverWrapper} />

### Popover Props

<Props of={Popover} />
