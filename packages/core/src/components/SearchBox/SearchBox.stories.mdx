import { useState, useEffect } from 'react';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import { optionsArray, boxSize } from './SearchBox.stories.tsx';
import * as stories from './SearchBox.stories';
import { select } from '@storybook/addon-knobs';
import { SearchBox } from './SearchBox';

<Meta title="Core" component={SearchBox} />

# SearchBox

### Add a auto suggest text box

<Preview withToolbar>
    <Story name="SearchBox">
        {() => {
            const [options, setOptions] = useState([]);
            const onSearchInputChangeHandler = value => {
                setOptions(value ? [...optionsArray] : []);
            };
            return (
                <SearchBox
                    placeholder="Search"
                    onSearchInputChange={onSearchInputChangeHandler}
                    options={options}
                    boxSize={select('boxSize', boxSize, 'S')}
                />
            );
        }}
    </Story>
</Preview>

### Props

<Props of={SearchBox} />

##### Small search text box

<Preview>
    <SearchBox boxSize="S" />
</Preview>

##### Medium search text box

<Preview>
    <SearchBox boxSize="M" />
</Preview>

#### Pending things in story

-   [ ] maxHeight
-   [ ] boxShadow
-   [ ] UI Styles
-   [ ] keyboard selection
-   [ ] onFocus and onBlur
-   [x] adding useMemo
-   [ ] onOptionSelected
-   [ ] introduce size in storybook
-   [ ] publish in storybook for feedback
-   [ ] unit tests
-   [ ] remove any
-   [ ] refactor
