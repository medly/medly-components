import { Label } from './Label';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './Label.stories';

<Meta title="Core" component={Label} />

# Label

The Label component lets you add captions to any element in your project. You can click or tap the label to activate the associated element. 

<Preview withToolbar>
    <Story name="Label">{stories.Basic()}</Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={Label} />

## LabelTheme

This component offers the following themes mentioned in the table below:

<Props of={stories.ThemeInterface} />
