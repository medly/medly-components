import { MultiSelect } from './MultiSelect';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './MultiSelect.stories';

<Meta title="Core" component={MultiSelect} />

# MultiSelect

### Basic MultiSelect component

<Preview withToolbar>
    <Story name="MultiSelect">{stories.Basic()}</Story>
</Preview>

### Props

<Props of={MultiSelect} />

### SelectTheme

This theme is shared between single select and multi select.

<Props of={stories.ThemeInterface} />
