import { HTMLProps } from '@medly-components/utils';
import { HomeIcon } from '@medly-components/icons';
import { Card } from './Card.tsx';
import Text from '../Text';
import { withKnobs, text, color, select, boolean } from '@storybook/addon-knobs';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { variants, display, itemsPosition, flowDirection, ThemeInterface } from './Card.stories';

<Meta title="Core" component={Card} parameters={{ jest: ['Card.test.tsx'] }} />

# Card

A card component is a content container for images, text, and actions in the context of a single subject. The hover effect works only when you pass the 'onClick' prop.
You can play with the props in the **Canvas** tab. 

<Preview withToolbar>
    <Story name="Card" decorators={[withKnobs]}>
        <Card
            variant={select('Variant', variants, 'solid')}
            flowDirection={select('Flow Direction', flowDirection, 'horizontal')}
            alignItems={select('Items Position', itemsPosition, 'center')}
            display={select('Display', display, 'flex')}
            separator={boolean('Separator', true)}
            withoutPadding={boolean('Without Padding', true)}
            fullWidth={boolean('Full Width', false)}
            fullHeight={boolean('Full Height', false)}
            minWidth={text('Min Width', '')}
            minHeight={text('Min Height', '')}
            onClick={action('Clicked')}
        >
            <Card variant="flat" display="flex" alignItems="center">
                <Text textWeight="Strong" textVariant="h2">
                    8
                </Text>
                <Text>Marks</Text>
            </Card>
            <Card variant="flat" display="flex" flex={6} alignItems="left">
                <Text>17607124</Text>
                <Text textVariant="h3">John, Snow</Text>
                <Text>Son of Lyanna Stark and Rhaegar Targaryen, the late Prince of Dragonstone</Text>
            </Card>
        </Card>
    </Story>
</Preview>

> **Note**: Shadow on hovering appears when you pass the `onClick` prop to it.

## Variants

Use the variant prop to change the card style.

<Preview>
    <Card variant="solid">
        <Text>Solid Card</Text>
    </Card>
    <Card variant="flat">
        <Text>Flat Card</Text>
    </Card>
</Preview>

## Children direction

To customize the card component in multiple ways, use `display='flex'`.

<Preview>
    <Card display="flex" flowDirection="horizontal" alignItems="center">
        <HomeIcon />
        <Text>Horizontal</Text>
    </Card>
    <Card display="flex" flowDirection="vertical">
        <HomeIcon />
        <Text>Vertical</Text>
    </Card>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={Card} />

## CardTheme

This component offers the following themes mentioned in the table below:

<Props of={ThemeInterface} />
