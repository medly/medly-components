import { Link } from './Link';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './Link.stories';

<Meta title="Core" component={Link} />

# Link

The Link component creates links and manages navigation in your application.

<Preview withToolbar>
    <Story name="Link">{stories.Basic()}</Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={Link} />

## LinkTheme

This component offers the following themes mentioned in the table below:

<Props of={stories.ThemeInterface} />
