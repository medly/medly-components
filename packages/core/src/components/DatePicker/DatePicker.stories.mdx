import { DatePicker } from './DatePicker';
import { defaultTheme } from '@medly-components/theme';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './DatePicker.stories';

<Meta title="Core/DatePicker" component={DatePicker} />

# DatePicker

We are using `@datepicker-react/styled` underneath with some modification.

<Story name="Basic">{stories.Basic}</Story>

### DatePicker Props

<Props of={DatePicker} />

### ReactDatepicker theme

You can check the all posible values [here](https://github.com/tresko/react-datepicker#theming)

### ReactDatepicker defaults

<code>{JSON.stringify(defaultTheme.reactDatepicker, null, 2)}</code>
