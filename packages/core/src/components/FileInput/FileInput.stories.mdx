import { FileInput } from './FileInput';
import { defaultTheme } from '@medly-components/theme';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import * as stories from './FileInput.stories';

<Meta title="Core" component={FileInput} />

# FileInput

The `FileInput` component enables uploading files from the local storage.

<Preview withToolbar>
    <Story name="FileInput">{stories.Basic()}</Story>
</Preview>

## Props

This component offers the following props mentioned in the table below:

<Props of={FileInput} />

## FileInputTheme

This component offers the same theme as Input. To view the same click [here] (http://www.medlycomponents.com/?path=/docs/core--input#inputtheme)

