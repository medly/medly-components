import { Toast } from './Toast';
import { Preview, Story, Meta, Props } from '@storybook/addon-docs/blocks';
import { variants, ThemeInterface } from './Toast.stories';
import { withKnobs, select, text } from '@storybook/addon-knobs';

<Meta title="Core" component={Toast} />

# Toast

### Basic Toast

<Preview withToolbar>
    <Story name="Toast" decorators={[withKnobs]}>
        <Toast
            variant={select('Variant', variants, 'success')}
            header={text('Header', 'Default Notification')}
            message={text(
                'Message',
                'If there is a long description that breaks to multiple lines, then the height of the component should increase by the line height of the added text.'
            )}
        ></Toast>
    </Story>
</Preview>

### Props

<Props of={Toast} />

### ToastTheme

<Props of={ThemeInterface} />
